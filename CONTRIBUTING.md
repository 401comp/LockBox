# Contributing

Bug reports and focused pull requests are welcome. Please describe the macOS
version, Python version, and steps to reproduce. Keep changes small, testable,
and free of generated build output.

Run the self-test before opening a pull request:

```bash
.venv/bin/python lockbox.py --self-test
```
